import AppShell from "../../components/AppShell";
import LivePageContent from "../../components/LivePageContent";

export default function LivePage() {
  return (
    <AppShell title="Live" subtitle="Events, activity, global ladder">
      <LivePageContent />
    </AppShell>
  );
}
